/**
 * @generated SignedSource<<%= hash %>>
 * @flow
 */

import type {TurboModule} from 'react-native/Libraries/TurboModule/RCTExport';
import {TurboModuleRegistry} from 'react-native';

export interface Native<%= name %> extends TurboModule {
<% methods.split(',').filter(m => m.trim()).forEach(method => { %>
  <%= method.trim() %>(args: { [key: string]: any }): Promise<void>;
<% }); %>
<% if (constant) { %>
  getConstants(): { [key: string]: any };
<% } %>
}

export default (TurboModuleRegistry.get<Native<%= name %>>("<%= name %>"): ?Native<%= name %>);